{
  "name": "json-visualizer-root",
  "version": "1.0.0",
  "private": true,
  "description": "JSON Visualizer - Root package for deployment",
  "scripts": {
    "build": "cd json-visualizer && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
